# 🛠️ Инструкции по установке и настройке

Это руководство поможет вам настроить окружение для работы с ML Cheatsheets.

## 📋 Содержание

- [Системные требования](#системные-требования)
- [Базовая установка](#базовая-установка)
- [Установка для упражнений](#установка-для-упражнений)
- [Установка для анимаций](#установка-для-анимаций)
- [Проверка установки](#проверка-установки)
- [Решение проблем](#решение-проблем)

## 💻 Системные требования

### Минимальные требования

- **Python**: версия 3.8 или выше
- **pip**: последняя версия
- **Память**: минимум 4 GB RAM
- **Место на диске**: минимум 2 GB свободного места

### Рекомендуемые требования

- **Python**: версия 3.10 или выше
- **Memory**: 8 GB RAM или больше
- **GPU**: Для обучения нейросетей рекомендуется NVIDIA GPU с CUDA

## 🚀 Базовая установка

### 1. Клонирование репозитория

```bash
# HTTPS
git clone https://github.com/gurovic/MLCheatSheets.git

# SSH (если настроен SSH ключ)
git clone git@github.com:gurovic/MLCheatSheets.git

cd MLCheatSheets
```

### 2. Создание виртуального окружения (рекомендуется)

#### Используя venv (встроенный в Python)

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать окружение
# На Windows:
venv\Scripts\activate

# На macOS/Linux:
source venv/bin/activate
```

#### Используя conda (альтернатива)

```bash
# Создать окружение с Python 3.10
conda create -n mlcheatsheets python=3.10

# Активировать окружение
conda activate mlcheatsheets
```

### 3. Обновление pip

```bash
python -m pip install --upgrade pip
```

## 📚 Установка для упражнений

Для выполнения практических упражнений установите основные библиотеки:

```bash
# Установить все зависимости из requirements.txt
pip install -r requirements.txt
```

### Что будет установлено:

- **pandas** - работа с табличными данными
- **numpy** - численные вычисления
- **scikit-learn** - алгоритмы машинного обучения
- **matplotlib** - визуализация данных
- **seaborn** - красивые графики
- **scipy** - научные вычисления
- **imbalanced-learn** - работа с несбалансированными данными

### Проверка установки для упражнений

```bash
python -c "import pandas, numpy, sklearn; print('Все библиотеки установлены успешно!')"
```

## 🎨 Установка для анимаций

Если вы хотите создавать или модифицировать анимации:

### Вариант 1: Только matplotlib (легковесный)

```bash
# Уже включено в основной requirements.txt
pip install matplotlib seaborn
```

### Вариант 2: С Manim (для продвинутых анимаций)

#### На Ubuntu/Debian

```bash
# Установить системные зависимости
sudo apt update
sudo apt install -y \
    libcairo2-dev \
    libpango1.0-dev \
    ffmpeg \
    texlive \
    texlive-latex-extra

# Установить Python зависимости
pip install -r animations/requirements.txt
```

#### На macOS

```bash
# Установить через Homebrew
brew install cairo pango ffmpeg

# Установить Python зависимости
pip install -r animations/requirements.txt
```

#### На Windows

```bash
# Установить через chocolatey (если установлен)
choco install miktex ffmpeg

# Или скачать вручную:
# - FFmpeg: https://ffmpeg.org/download.html
# - MiKTeX: https://miktex.org/download

# Установить Python зависимости
pip install -r animations/requirements.txt
```

### Проверка установки Manim

```bash
manim --version
```

## ✅ Проверка установки

### Базовая проверка

Создайте файл `test_setup.py`:

```python
#!/usr/bin/env python
# -*- coding: utf-8 -*-

import sys

def test_imports():
    """Проверка импорта основных библиотек"""
    
    print("Проверка версии Python...")
    print(f"Python версия: {sys.version}")
    
    print("\nПроверка импорта библиотек...")
    
    libraries = {
        'numpy': 'NumPy',
        'pandas': 'Pandas',
        'sklearn': 'Scikit-learn',
        'matplotlib': 'Matplotlib',
        'seaborn': 'Seaborn',
    }
    
    success = []
    failed = []
    
    for module, name in libraries.items():
        try:
            __import__(module)
            version = __import__(module).__version__
            success.append(f"✅ {name}: {version}")
        except ImportError:
            failed.append(f"❌ {name}: не установлен")
    
    print("\nУспешно установлены:")
    for item in success:
        print(item)
    
    if failed:
        print("\nОшибки:")
        for item in failed:
            print(item)
        return False
    
    print("\n🎉 Все библиотеки установлены успешно!")
    return True

if __name__ == "__main__":
    success = test_imports()
    sys.exit(0 if success else 1)
```

Запустите:

```bash
python test_setup.py
```

### Проверка упражнений

Попробуйте открыть одно из упражнений:

```python
import pandas as pd
import numpy as np
from sklearn.preprocessing import StandardScaler

# Загрузить данные из упражнения
data = pd.read_csv('exercises/preprocessing/input.csv')
print("Данные загружены успешно!")
print(data.head())
```

## 🔧 Решение проблем

### Проблема: pip не найден

```bash
# Установите pip
python -m ensurepip --upgrade
```

### Проблема: Ошибка при установке библиотек

```bash
# Попробуйте обновить pip и setuptools
pip install --upgrade pip setuptools wheel

# Затем установите требования снова
pip install -r requirements.txt
```

### Проблема: Конфликты версий

```bash
# Создайте новое чистое виртуальное окружение
python -m venv venv_new
source venv_new/bin/activate  # или venv_new\Scripts\activate на Windows
pip install -r requirements.txt
```

### Проблема: Нет прав доступа (Linux/macOS)

```bash
# Используйте виртуальное окружение вместо установки глобально
# Или используйте --user флаг
pip install --user -r requirements.txt
```

### Проблема: ImportError для специфичных пакетов

```bash
# Установите пакет отдельно
pip install имя_пакета

# Или переустановите
pip install --force-reinstall имя_пакета
```

### Проблема: Медленная установка

```bash
# Используйте зеркало PyPI (для России)
pip install -r requirements.txt -i https://pypi.org/simple/
```

## 🚀 Дополнительная настройка

### Jupyter Notebook (опционально)

Если вы хотите работать с упражнениями в Jupyter:

```bash
pip install jupyter notebook jupyterlab

# Запустить Jupyter Notebook
jupyter notebook

# Или JupyterLab
jupyter lab
```

### VS Code (рекомендуемая IDE)

1. Установите [Visual Studio Code](https://code.visualstudio.com/)
2. Установите расширение Python
3. Откройте папку MLCheatSheets
4. Выберите интерпретатор Python из виртуального окружения

### PyCharm (альтернативная IDE)

1. Установите [PyCharm](https://www.jetbrains.com/pycharm/)
2. Откройте проект MLCheatSheets
3. Настройте интерпретатор Python на ваше виртуальное окружение

## 🐳 Docker (продвинутые пользователи)

Если вы предпочитаете использовать Docker:

```dockerfile
# Создайте Dockerfile
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python"]
```

```bash
# Соберите образ
docker build -t mlcheatsheets .

# Запустите контейнер
docker run -it --rm -v $(pwd):/app mlcheatsheets
```

## 📞 Получение помощи

Если у вас возникли проблемы:

1. Проверьте [Issues](https://github.com/gurovic/MLCheatSheets/issues) - возможно, проблема уже решена
2. Создайте новый Issue с описанием проблемы
3. Укажите:
   - Версию Python
   - Операционную систему
   - Текст ошибки
   - Шаги для воспроизведения

## 📚 Следующие шаги

После успешной установки:

1. 📖 Прочитайте [ROADMAP.md](ROADMAP.md) для выбора пути обучения
2. 💪 Начните с [упражнений](exercises.md)
3. 📚 Изучайте [cheatsheets](README.md)

---

© 2024 ML Cheatsheets. Автор: Владимир Гуровиц (школа "Летово")
